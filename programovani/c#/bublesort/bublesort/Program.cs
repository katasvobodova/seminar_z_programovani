using System;

namespace bublesort
{
    internal class Program
    {
        static void Main(string[] args)
        {
        List<int> pole = new List<int>();
        string[] Console.ReadLine();
        for (int n = 0; n <= pole.Length; n++)
            pole[n] = hodnota;
            pole[n - 1] = hodnota_n_minus_jedna;
            if 
                hodnota > hodnota_n_minus_jedna;
        }
    }
}
